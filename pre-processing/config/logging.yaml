version: 1
disable_existing_loggers: False
formatters:
    simple:
        #format: "%(asctime)s %(levelname)-6s [%(filename)s:%(lineno)4s - %(funcName)25s()] %(message)s"
        format: "%(asctime)s  [%(filename)s:%(lineno)4s] %(levelname)s %(message)s"
        datefmt: "%Y-%m-%d %H:%M:%S"

handlers:
    console:
        class: logging.StreamHandler
        level: DEBUG
        formatter: simple
        stream: ext://sys.stdout

    info_file_handler:
        class: logging.handlers.RotatingFileHandler
        level: INFO
        formatter: simple
        filename: /preprocessedData/log/preprocessing/info.log
        maxBytes: 10485760 # 10MB
        backupCount: 20
        encoding: utf8

    error_file_handler:
        class: logging.handlers.RotatingFileHandler
        level: ERROR
        formatter: simple
        filename: /preprocessedData/log/preprocessing/errors.log
        maxBytes: 10485760 # 10MB
        backupCount: 20
        encoding: utf8

loggers:
    my_module:
        level: INFO
        handlers: [console]
        propagate: no

root:
    level: INFO
    handlers: [console, info_file_handler, error_file_handler]
